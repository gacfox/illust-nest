server:
  port: 8080
  mode: debug

database:
  driver: sqlite
  path: ./data/illust-nest.db

jwt:
  secret: dev-secret-key-change-in-production
  expire_hours: 168

web:
  static_dir: ./frontend/dist
